<div>
  <h4>Comment by <%= comment.author_name %></h4>
  <p class="comment"><%= comment.body %></p>
	<p>Posted <%= distance_of_time_in_words(comment.created_at, Time.now) %> ago
	<% if logged_in? %> | <%= link_to "delete", [comment.article, comment], method: :delete, data: { confirm: 'Delete this comment?'}%>
	<% end %></p>
	<p> <%# = link_to "delete", [comment.article, comment], method: :delete, data: { confirm: 'Delete this comment?'}%> </p>
</div>

